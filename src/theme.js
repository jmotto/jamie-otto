import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: "#004d40",
            light: ""
        },
        secondary:{
            main: '',
        },
        otherColor:{
            main:""
        }
    }


})

// to use add themeProvider tag around App in index.js